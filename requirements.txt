# Python 3.11+ required (the project was developed on 3.14).
#
# scikit-learn is pinned exactly: the committed models/*.joblib pipelines were
# trained with 1.9.0, and loading them under a different minor version raises
# InconsistentVersionWarning and may change predictions.
scikit-learn==1.9.0

# Everything else only needs a floor, not an exact pin.
pandas>=2.0
numpy>=1.26
matplotlib>=3.8
seaborn>=0.13
joblib>=1.3

# Streamlit demo (app.py) only; not needed to run the notebook.
streamlit>=1.40
